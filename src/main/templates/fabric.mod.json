{
    "schemaVersion": 1,
    "id": "${mod_id}",
    "name": "${mod_name}",
    "version": "${mod_version}",
    "description": "${mod_description}",
    "environment": "client",
    "entrypoints": {
        "main": [
            "dev.celestial.silly.loaders.fabric.FabricEntrypoint"
        ]
    },
    "mixins": [
        {
            "config": "sillyplugin.mixins.json",
            "environment": "*"
        }
    ],
    "accessWidener": "sillyplugin.accesswidener"
}